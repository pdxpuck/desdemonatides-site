# Assets placeholder

Save SLF's Etsy shop banner here as **banner.jpg**
(right-click the banner on the Etsy shop page -> Save image as).

Optional: **avatar.jpg** from her Instagram.

- `banner.jpg` — fills the full-width banner slot at the top of `index.html`
  - **DONE 2026-09-22** — wired from user-dropped file `isbl_3360x840.86664725_68kqmj7m.jpg`
    (genuine JPEG, 3360×840, ~150 KB). Renamed to `banner.jpg`; `index.html` referenced
    `assets/banner.jpg` already, so no HTML change was needed.
- `avatar.jpg` — **PENDING** — reserved for future use (no profile-photo slot in the page yet)

Until `banner.jpg` exists, `index.html` shows a gradient placeholder block with
the wordmark, so the page stays preview-able (and sign-off-able) without the
real art.
